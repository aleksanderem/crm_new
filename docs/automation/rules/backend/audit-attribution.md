# Audit Attribution

If system-driven flows still require a concrete user id for activity or audit attribution, preserve the real trigger source in structured details such as `source: "sms_reply"`.

If the codebase lacks a first-class system actor model, document the attribution compromise explicitly instead of hiding it.
