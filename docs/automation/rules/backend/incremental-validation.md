# Incremental Validation

For backend-only work, prefer validating against `convex/tsconfig.json` during implementation so iteration stays fast and scoped.

Full repo lint, typecheck, and broader tests remain pre-review targets.
