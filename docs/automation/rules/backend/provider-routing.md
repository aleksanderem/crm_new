# Provider Routing

If inbound processing needs to identify organization context from the provider recipient, use indexed configuration lookup paths and provider-specific normalization.

Do not scan or infer organization context ad hoc.
