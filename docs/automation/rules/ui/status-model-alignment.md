# Status Model Alignment

When backend supports a status that UI currently omits, align UI with backend truth instead of creating a frontend-only alternative status model.

This matters especially for Gabinet appointment states such as `pending_confirmation`.
