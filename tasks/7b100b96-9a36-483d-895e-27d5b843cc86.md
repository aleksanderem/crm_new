Task ID: 7b100b96-9a36-483d-895e-27d5b843cc86
Board: CRM + Gabinet SaaS
Title: Tabele: Inline editing — edycja komórek bezpośrednio w tabeli
Status: in_progress

Timestamp: 2026-03-06T17:40:00+01:00
Author: agent (Hugo)

Initial comment:
- I started the task and explored the data-table components.
- Found existing DataTable (src/components/data-table/data-table.tsx) and CrmDataTable (src/components/crm/enhanced-data-table.tsx).
- There were helper files for inline editing examples and cell primitives; I added missing implementations for editable-columns and ensured editable-cell exists.

Changes made:
- Added src/components/data-table/editable-cell.tsx (inline editing UI for text/number/select/date/time/datetime/boolean, with validation, save/cancel, optimistic UI handling).
- Added src/components/data-table/editable-columns.tsx (createEditableColumn helper and editablePresets for common field types).
- Added src/components/data-table/editable-examples.tsx showing how to wire inline editing for Contacts and Patients.
- Committed changes on branch feat/gabinet-employee-schedules.

Planned next steps:
1. Integrate inline editing into key tables across CRM and Gabinet (contacts, patients, employees, appointments where applicable).
2. Add tests for EditableCell behavior (keyboard handling, validation, save/cancel flows).
3. Provide a migration guide for devs: how to convert existing column defs to createEditableColumn.
4. Roll out incrementally and instrument for failure rates (to revert to panel editor if needed).

Notes/questions:
- The EditableCell auto-saves on blur and Enter; booleans auto-toggle. Confirm this UX is acceptable.
- For complex multi-field edits we might still prefer side panels — do we keep both patterns? I recommend keeping side panels for bulk/complex edits and using inline editing for quick edits.

I'll start converting a few tables (Contacts, Patients) to use the editable columns and open a PR with screenshots.
