Task ID: d4884fc4-3b6f-43f7-83d4-61d8452900c6
Board: CRM + Gabinet SaaS
Title: Przeprojektuj komponent BodyChart — zamień prymitywne SVG na anatomiczne ścieżki
Status: review

Timestamp: 2026-03-06T17:46:00+01:00
Author: agent (Hugo)

Initial review comment:
- I opened the task and inspected src/components/gabinet/BodyChart.tsx.
- I also inspected the anatomical path sources under src/components/gabinet/body-parts/body-front.ts and body-back.ts.

Findings
- The project already contains anatomical SVG paths (bodyFront/bodyBack) that appear to be sourced from react-native-body-highlighter (viewBoxes and outlines present).
- BodyChart.tsx already uses bodyFront/bodyBack and maps parts into SVG <path> elements, building both background silhouette and clickable colored overlays.
- The BodyView component supports left/right/common subpaths; it applies fills, opacities, hover states and click handlers; the side panel editor is already wired to selected region, color and intensity.

Conclusion
- The core migration requested by the task (replace primitive shapes with anatomical paths from react-native-body-highlighter) has already been implemented.
- This task is correctly in REVIEW state. I recommend the following review checklist before merging:
  1. Visual QA: Compare previous (primitive) vs new anatomical rendering at multiple viewport widths and device pixel ratios.
  2. Accessibility: Ensure each clickable region has accessible name/aria-labels and keyboard focusability (SVG <path> elements need role and tabindex handling).
  3. Performance: Large numbers of paths may impact rendering; verify CPU/GPU usage and consider combining common/background paths into a single <path> where possible.
  4. Hit target: Small paths (hands/feet) can be hard to click on mobile — consider adding an invisible padding path for easier tapping or increase pointer area via strokeWidth on hover.
  5. i18n keys: Body region labels are referenced via t('gabinet.bodyChart.regions.<id>') — ensure all ids match the slug values used in bodyFront/bodyBack.
  6. Tests & Storybook: Add a visual story with controls for color/intensity and a snapshot test of the SVG markup.
  7. Licensing: Confirm react-native-body-highlighter license is compatible and attribution is included if required.

Next actions I can take (pick any):
- Run a visual QA and capture screenshots (I can start the dev server and open the component in a story or example page).
- Add accessibility attributes (role="button", tabindex, aria-label) and keyboard handlers to each clickable overlay path.
- Add a small helper to expand hit area for small regions for better mobile tap targets.
- Add a Storybook story + visual snapshot test.
- If you prefer, I can mark the task as done and leave it to reviewers.

Files inspected:
- src/components/gabinet/BodyChart.tsx
- src/components/gabinet/body-parts/body-front.ts
- src/components/gabinet/body-parts/body-back.ts

I will hold and wait for your preferred next action. If you don't respond, I'll add accessibility (aria/keyboard) and increase touch targets for small regions, then push changes and post screenshots.
